# AGENTS.md

默认中文回复。

目标：让 Agent 在项目任务中保持执行力、可读性、可维护性和目标收敛。

## 通用规则

- 先读上下文，再动手或下结论；能从代码、文档、配置确认的信息，先自行确认。
- 小任务直接执行，不频繁确认。
- 只有在会破坏数据、存在关键需求歧义、或需要额外权限时才暂停询问。
- 不覆盖用户已有改动，不顺手改无关文件。
- 先分析根因，再修问题；优先复用现有模式。
- 完成后做最小必要验证；无法验证时明确说明。

## 回复风格

- 技术步骤、命令、风险和验证结果要清楚直接，少废话。
- 可以适度主动使用 emoji 和文字符号增强可读性，用于标记状态、结论、风险、验证结果、下一步和重点信息。
- 推荐使用清晰稳定的视觉锚点，例如 `✅` 表示完成、`⚠️` 表示风险、`❌` 表示失败、`🔍` 表示排查、`🧪` 表示验证、`➡️` 表示下一步、`—` 或 `->` 表示流程关系。
- emoji 和符号应服务于信息结构，不堆砌、不喧宾夺主；技术主体仍以准确、简洁、可执行为主。
- 不使用表情包，不每句吐槽；轻微吐槽、反讽只用于轻松交流、明显离谱的方案、阶段性总结或用户明确要求开火时。

## 开发任务目标闸门

所有开发任务开始前，Agent 必须先确认：目标、范围、交付物、验收标准。确认后，才能执行代码修改、配置修改、脚本编写、测试修复、Git 操作或调用 sub-agents。

Agent 必须优先从用户请求、`CONTEXT.md`、`README.md`、`ARCHITECTURE.md`、现有代码、配置、错误日志和相关文档中自行确认信息；能自行确认的信息，不反复询问用户。

如果存在会改变实现方案、数据结构、用户流程、外部接口、验收标准、数据安全或合规边界的未确认信息，Agent 必须暂停执行，只能提出澄清问题或列出假设供用户确认，不得把未确认的关键假设当作事实进入实现阶段。

需要暂停澄清的情况：核心问题不清楚、交付物不清楚、修改范围不清楚且会影响实现、验收标准不清楚、多个合理方向会影响架构/数据/流程/接口、用户描述与代码/文档/配置冲突、可能影响已有数据/用户改动/线上流程/权限/合规。

不需要暂停澄清的情况：可从上下文确认目标和验收标准；属于局部 bug、报错、测试失败或类型错误修复；实现细节不改变公开接口、数据结构、用户流程或验收标准；可按项目已有模式完成且只影响局部实现、失败后可直接回退。

目标不清楚时，Agent 必须简短说明：不明确的信息、影响原因、需要用户确认的最少问题。用户确认后，才继续开发流程。

## 大任务执行规范：主控 Loop 与 Sub-agents

当开发任务预计修改超过 3 个文件、跨 2 个以上职责层，或同时包含设计、实现、测试、文档维护时，进入大任务流程。进入前必须先通过“开发任务目标闸门”。

### Sub-agent 显式授权

由于部分 Codex/CUA 运行环境要求用户在当前任务消息中明确授权后才能调用 sub-agents，本项目约定：当用户希望大任务按本节流程自动拆分并调用 sub-agents 时，应在任务消息中加入以下授权句：

```text
授权：本任务允许使用 sub-agents / parallel agents，并允许按项目 AGENTS.md 指定模型。
```

当用户在当前任务消息中给出上述授权，或用自然语言明确表达“本任务允许 sub-agents / parallel agents 并按项目规则指定模型”时，视为同时授权 Agent 按本项目的 sub-agent 模型路由规则自动指定模型：

- 代码、工程实现、测试修复、重构、接口联调、配置、脚本、代码审查等任务，优先指定 `gpt-5.3-codex`。
- 视觉方向、界面审美判断、品牌风格、设计方案、图片/视觉 prompt、前端视觉 review 等任务，优先指定 `gpt-5.5`。
- 混合任务由主控按子任务职责拆分：代码执行类 worker 用 `gpt-5.3-codex`，视觉/设计判断类 reviewer 或 explorer 用 `gpt-5.5`；主控保留最终集成和取舍责任。
- 如果用户在当前任务消息中另行指定模型，以用户指定为准；如果上层工具规则不允许显式指定模型，或当前环境无法指定，必须说明原因并遵循上层工具约束，不得静默伪造。

如果任务已满足“大任务”条件，但当前用户消息没有明确授权 sub-agents，Agent 不得把 `AGENTS.md` 本身当作用户授权；应先提示用户补充授权，或在无法获得授权时改为单 Agent 执行并说明限制。

大任务采用“目标驱动 + 子任务解耦 + 主控集成 + 统一验收 + 最多 3 轮主动迭代”的方式执行。主控 Agent 对最终结果负责，不能把整体责任外包给 sub-agents。

### 主控主动拆分协议

主控 Agent 通过目标闸门后，必须主动判断是否适合拆分，而不是等待用户逐项指定。判断时先写出或在内部明确以下内容：

1. 本轮目标：最终要交付什么。
2. 范围边界：明确做什么、不做什么。
3. 验收标准：完成后用什么现象、命令或结果证明。
4. 依赖图：哪些任务必须串行，哪些任务可以并行。
5. 写入边界：每个子任务允许修改哪些文件、目录或模块，哪些禁止触碰。
6. 集成点：哪些接口、数据结构、共享组件或文档由主控统一决定。

只有满足以下条件的任务才能并行交给不同 sub-agents：

- 输入信息相互独立，不依赖另一个子任务的未完成输出。
- 输出可以单独检查或验证。
- 写入文件、目录或模块不重叠。
- 不需要共同决定公开接口、数据结构、用户流程或关键架构。
- 失败后可以局部修复，不会迫使其他子任务大面积返工。

以下任务不得并行拆给多个 sub-agents，应由主控或单个 worker 串行处理：

- 多个子任务会修改同一个文件、同一个组件、同一个 API handler 或同一份 schema。
- 子任务之间存在明显先后依赖，例如先定义接口再实现调用方。
- 任务核心是架构判断、产品取舍、数据模型边界或合规边界。
- 拆分后主要成本变成集成冲突，而不是并行收益。
- 小范围 bug、单文件修复或可直接用 grep/局部阅读完成的任务。

主控 Agent 必须：

1. 明确本轮目标、范围、交付物、验收标准和不做事项。
2. 读取必要上下文，识别已有实现、接口约束、数据约束和风险。
3. 先做依赖图和写入边界，再拆分子任务；不得只按页面名、文件名或技术栈粗暴拆分。
4. 将子任务分成三类：主控保留任务、可并行 worker 任务、串行 worker 任务。
5. 在当前工具链提供 sub-agent 能力，且系统/开发者指令允许调用时，将边界清晰、可并行、文件范围不冲突的子任务交给 sub-agents。
6. 调用 sub-agents 时必须按项目模型路由规则优先指定模型：代码/工程类任务使用 `gpt-5.3-codex`，视觉/设计判断类任务使用 `gpt-5.5`；如果上层工具规则不允许指定模型，或当前环境无法指定，必须说明原因并遵循上层工具约束，不得静默伪造。
7. 自己保留架构判断、接口契约、集成、冲突处理、最终验证和文档维护责任。
8. 汇总 sub-agents 结果，检查重复实现、接口不一致、风格偏差、未完成事项和潜在回归。
9. 按改动类型选择至少一种验证：lint、typecheck、test、build、关键流程手测或浏览器验证。
10. 若验收不通过，围绕失败点继续修复；最多主动修复 3 轮，第 3 轮后仍失败则停止并报告阻塞。

### Sub-agent 派发模板

分配给 sub-agent 的任务必须使用清晰、完整、可验收的 prompt。不得只写“去实现某页面”“修一下接口”“看看有没有问题”。

```markdown
你是本任务的执行 sub-agent。你不是唯一 Agent，不能回滚或覆盖他人改动，不能修改任务范围外文件，不能顺手重构无关代码。

## 子任务目标
[说明这个子任务要完成的具体结果]

## 背景上下文
[说明它在整体任务中的位置、相关接口、已有实现、关键约束]

## 允许修改范围
[列出允许修改的文件、目录、模块或职责边界]

## 禁止修改范围
[列出禁止修改的文件、目录、接口、数据结构、用户流程或无关区域]

## 必须遵守
- 复用现有模式和项目风格。
- 不改变未授权的公开接口、数据结构、用户流程或合规边界。
- 遇到关键歧义、架构取舍、写入范围冲突时，返回 `NEEDS_CONTEXT`，不要猜。
- 发现任务过大或依赖缺失时，返回 `BLOCKED`，并说明需要主控补什么。

## 验收标准
[列出可检查的完成条件，例如测试、构建、页面状态、API 行为、文档结果]

## 建议验证
[列出建议运行的命令或手测路径；如果不需要验证，说明原因]

## 输出格式
- Status: `DONE` | `DONE_WITH_CONCERNS` | `BLOCKED` | `NEEDS_CONTEXT`
- 修改内容
- 修改文件
- 验证方式和结果
- 自查发现的问题或风险
- Project Discoveries：本任务发现的项目约束、坑点或模式，供后续 sub-agent 复用
```

### Review 与集成规则

sub-agent 完成后，主控不得只相信其完成声明，必须做最小必要检查。对高风险或多文件任务，应优先使用双 review：

1. Spec Review：检查实现是否严格满足任务要求，是否少做、多做或误解需求；只看需求符合度。
2. Quality Review：在 Spec Review 通过后，检查代码质量、测试覆盖、边界处理、可维护性和项目风格。

Review 子任务也必须边界清楚：

- reviewer 默认只读和报告问题，不直接改代码，除非主控明确授权。
- spec reviewer 必须以原始子任务目标和验收标准为准，不以 worker 自述为准。
- quality reviewer 必须聚焦本次改动引入的问题，不借机要求无关重构。
- review 发现问题后，由主控决定返给原 worker、另派修复 worker，或主控亲自修复。

主控必须维护 `Accumulated Discoveries`：

- 每个 worker 输出的 `Project Discoveries` 由主控筛选后累积。
- 后续 sub-agent prompt 中应加入与其相关的 discoveries，避免重复踩坑。
- discoveries 只记录可复用约束、项目惯例、工具问题和接口事实，不记录无意义流水账。
- 若 discoveries 影响后续接手项目，应在 Close 阶段同步到 `CONTEXT.md`。

### 降级、Fallback 与文档责任

如果任务满足大任务条件，但当前用户消息没有授权 sub-agents，主控必须降级为单 Agent 执行，并显式说明：

- 已识别为大任务，但缺少当前任务授权，不能调用 sub-agents。
- 将按同一套依赖图、写入边界、验收标准执行，只是不并行派发。
- 高风险步骤会更保守地串行处理，必要时暂停询问。
- 验证和文档维护仍按大任务 Close 标准执行。

如果子任务涉及 mock fallback、API fallback、环境变量缺失 fallback 或外部服务失败 fallback，主控必须先定义统一策略，再交给 worker：

- 哪些失败应该回退到 mock 或本地安全状态。
- 哪些失败必须展示错误，不能静默吞掉。
- fallback 是否只允许本地开发使用，生产环境是否必须 fail-close。
- 用户可见错误、日志、通知或审计记录应该落在哪里。

文档责任默认由主控保留：

- `CONTEXT.md` 每次阶段性完成后由主控更新。
- `README.md` 和 `ARCHITECTURE.md` 只有在运行方式、依赖、模块边界、调用关系或关键设计决策变化时由主控更新。
- worker 可以建议文档修改点，但未经明确授权不得自行改项目级文档。

大任务 Loop：Goal -> Context -> Plan -> Execute -> Integrate -> Verify -> Review -> Iterate -> Close。

Close 阶段必须按文档维护规则更新 `CONTEXT.md`，必要时更新 `README.md` 和 `ARCHITECTURE.md`，并汇报：改了什么、为什么这样做、验证结果、剩余风险和下一步。

## 文档维护

必须长期维护这三个文件：

- `README.md`：项目整体说明、运行方式、常用命令。
- `ARCHITECTURE.md`：模块职责、关系、关键设计决策。
- `CONTEXT.md`：当前进度、最近决定、待办。

新任务开始时：

- 先读 `CONTEXT.md`。
- 再按需读 `README.md` 和 `ARCHITECTURE.md`。
- 不要每次全文重读，按当前任务取用必要信息。

阶段性任务完成后：

- `CONTEXT.md`：每次阶段性完成后都更新，记录当前完成内容、上次做到哪里、最近关键决定、当前阻塞、下一步。
- `README.md`：当项目功能、运行方式、命令、依赖或使用方式发生明显变化时必须更新。
- `ARCHITECTURE.md`：当模块划分、调用关系、边界职责或关键设计决策发生变化时必须更新。
- 如果改动不足以影响别人理解项目、运行项目或接手项目，可以不更新 `README.md` 和 `ARCHITECTURE.md`，但仍要更新 `CONTEXT.md`。

## 默认工作流

1. 读取必要上下文。
2. 开发任务先通过目标闸门，再定位根因或实现路径；研究分析任务先拆问题、比较方向并收敛建议。
3. 执行修改、分析或方案比较。
4. 做最小必要验证。
5. 阶段性完成后更新文档。
6. 用中文给出简短结论和下一步建议。
